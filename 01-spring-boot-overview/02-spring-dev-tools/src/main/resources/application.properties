# Use wildcard "*" to expose all endpoints
# You can also expose individual endpoints using comma-delimited list
spring.application.name=Project
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# Exclude individual endpoints with a comma-delimited list
management.endpoints.web.exposure.exclude=health,info

info.app.name=My cool app
info.app.description=Introduction to spring boot actuator
info.app.version=1.0.0


#
# Define my crazy properties
#
coach.name = Mickey Mouse
team.name = The Mouse Club


#
# change spring boot embedded server port
#
server.port=7070


#
# set the context path of the application
# All requests should be prefixed with /mycoolapp
#
server.servlet.context-path=/mycoolapp
